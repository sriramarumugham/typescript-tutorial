function addTwo(num): number {
    //num is taken as any 
    num.toUpperCase();//takes it as string

  return num + 2;
}
//number
addTwo(2);
//string
addTwo("5");

function addOne(num:number): number {
    //num is taken as any 
    // num.toUpperCase();//takes it as string
  return num + 2;
}

addOne(1);
// addOne("1");//erro throws




let loginUser=(name: string, email: string , paid: boolean=false)=>{}

loginUser("h" , "a" );

// we are passing two vairables
// paid:boolean =false take false as default



export{};